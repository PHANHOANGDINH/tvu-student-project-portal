import{useEffect,useState}from'react';import{getRoleDashboard}from'../../api/dashboardApi';
const labels={totalUsers:'Tổng người dùng',admins:'Quản trị viên',lecturers:'Giảng viên',students:'Sinh viên',academicYears:'Năm học',semesters:'Học kỳ',subjects:'Môn học',classes:'Lớp học phần',groups:'Nhóm sinh viên',topics:'Đề tài',topicsPending:'Đề tài chờ duyệt',topicsApproved:'Đề tài đã duyệt',topicsRejected:'Đề tài bị từ chối',topicsRevision:'Đề tài cần sửa',requirements:'Yêu cầu nộp',submissions:'Bài nộp',onTime:'Nộp đúng hạn',late:'Nộp trễ',graded:'Đã chấm',ungraded:'Chưa chấm',submitted:'Đã nộp',notSubmitted:'Chưa nộp',waitingGrade:'Chờ chấm',openRequirements:'Đợt đang mở',unread:'Thông báo chưa đọc',revisions:'Yêu cầu chỉnh sửa',publishedGrades:'Điểm đã công bố'};
export default function RoleDashboardPage({role,title}){const[data,setData]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState('');const load=()=>{setLoading(true);getRoleDashboard(role).then(r=>setData(r.data)).catch(e=>setError(e.message)).finally(()=>setLoading(false))};useEffect(load,[role]);if(loading)return <div className="panel">Đang tải dashboard...</div>;return <div><div className="page-title row-between"><div><h2>{title}</h2><p>Tổng quan dữ liệu và hoạt động gần đây.</p></div><button className="btn-light" onClick={load}>Làm mới</button></div>{error&&<div className="alert error">{error}</div>}<div className="dashboard-stat-grid">{Object.entries(data?.stats||{}).map(([k,v])=><div className="stat-card" key={k}><div><span>{labels[k]||k}</span><strong>{v??0}</strong></div></div>)}</div>{data?.group&&<div className="panel"><h3>Nhóm của tôi</h3><p><strong>{data.group.name}</strong> · {data.group.classCode}</p><p>Đề tài: {data.group.topicTitle||'Chưa đăng ký'} · {data.group.topicStatus||'-'}</p></div>}<div className="dashboard-grid"><div className="panel"><h3>Hạn nộp sắp tới</h3>{(data?.upcoming||[]).map(x=><p key={x.id}><strong>{x.title}</strong> · {x.classCode} · {new Date(x.deadline).toLocaleString('vi-VN')}</p>)}{!data?.upcoming?.length&&<p>Không có hạn nộp sắp tới.</p>}</div><div className="panel"><h3>Hoạt động gần đây</h3>{(data?.recentActivity||data?.recentSubmissions||[]).map((x,i)=><p key={x.id||`${x.submissionId}-${i}`}>{x.title||x.type} · {x.status} · {new Date(x.createdAt||x.submittedAt).toLocaleString('vi-VN')}</p>)}{!(data?.recentActivity||data?.recentSubmissions||[]).length&&<p>Chưa có hoạt động.</p>}</div></div>{data?.publishedGrades?.length>0&&<div className="panel"><h3>Điểm đã công bố</h3>{data.publishedGrades.map(x=><p key={x.submissionId}>{x.title}: <strong>{x.totalScore}/{x.maxScore}</strong></p>)}</div>}</div>}
