import{useEffect,useState}from'react';import{useNavigate,useParams}from'react-router-dom';import{getStudentResult}from'../../api/gradingApi';
export default function SubmissionResultPage(){const{id}=useParams(),nav=useNavigate(),[data,setData]=useState(null),[error,setError]=useState('');useEffect(()=>{getStudentResult(id).then(r=>setData(r.data)).catch(e=>setError(e.message))},[id]);return <div><div className="page-title"><h2>Kết quả bài nộp</h2><p>{data&&`${data.groupName} · ${data.requirementTitle}`}</p></div>{error&&<div className="alert error">{error}</div>}{!data?<div className="panel">Đang tải...</div>:<><div className="panel"><h3>{data.grade?`Điểm: ${data.grade.totalScore}/${data.grade.maxScore}`:'Điểm chưa được công bố'}</h3>{data.feedback?.comment&&<p><strong>Nhận xét:</strong> {data.feedback.comment}</p>}{data.feedback?.revisionRequired&&<div className="alert error"><strong>Yêu cầu chỉnh sửa:</strong> {data.feedback.revisionReason}</div>}{data.grade?.scores?.map(x=><p key={x.criterionId}>{x.name}: {x.score}/{x.maxScore}{x.comment?` · ${x.comment}`:''}</p>)}</div>{data.canResubmit&&<button className="btn-primary" onClick={()=>nav(`/student/submission-requirements/${data.requirementId}/submit`)}>Nộp lại bài</button>}<div className="panel"><h3>Lịch sử review</h3>{data.history.map(h=><p key={h.id}>{new Date(h.createdAt).toLocaleString('vi-VN')} · {h.eventType} · {h.toStatus}{h.comment?` · ${h.comment}`:''}</p>)}</div></>}</div>}
