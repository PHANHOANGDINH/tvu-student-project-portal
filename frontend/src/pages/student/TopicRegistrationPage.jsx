import { useEffect,useState } from 'react';import { getMyGroup,getTopic,saveTopic } from '../../api/groupsApi';
export default function TopicRegistrationPage(){const empty={title:'',description:'',technologies:'',objectives:'',notes:''},[group,setGroup]=useState(null),[topic,setTopic]=useState(null),[form,setForm]=useState(empty),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{let active=true;async function load(){try{const g=(await getMyGroup()).data;if(!active)return;setGroup(g);try{const t=(await getTopic(g.id)).data;if(!active)return;setTopic(t);setForm(t)}catch(e){if(e.status!==404)throw e}}catch(e){if(active)setError(e.message)}finally{if(active)setLoading(false)}}void load();return()=>{active=false}},[]);const submit=async e=>{e.preventDefault();try{setError('');const t=(await saveTopic(group.id,form,!!topic)).data;setTopic(t);setForm(t)}catch(e){setError(e.message)}};if(loading)return <div className="panel">Đang tải...</div>;return <div><div className="page-title"><h2>Đăng ký đề tài</h2><p>Trưởng nhóm gửi đề tài để giảng viên phụ trách duyệt.</p></div>{error&&<div className="alert error">{error}</div>}{topic&&<div className="panel"><strong>Trạng thái: {topic.status}</strong>{topic.reviewComment&&<p>Phản hồi: {topic.reviewComment}</p>}</div>} {group&&<form className="panel" onSubmit={submit}><input required placeholder="Tên đề tài" value={form.title||''} onChange={e=>setForm({...form,title:e.target.value})}/><textarea required placeholder="Mô tả" value={form.description||''} onChange={e=>setForm({...form,description:e.target.value})}/><input placeholder="Công nghệ dự kiến" value={form.technologies||''} onChange={e=>setForm({...form,technologies:e.target.value})}/><textarea placeholder="Mục tiêu" value={form.objectives||''} onChange={e=>setForm({...form,objectives:e.target.value})}/><textarea placeholder="Ghi chú" value={form.notes||''} onChange={e=>setForm({...form,notes:e.target.value})}/><button className="btn-primary" disabled={topic&&!['PENDING','REQUIRES_REVISION'].includes(topic.status)}>Lưu đăng ký</button></form>}</div>}
