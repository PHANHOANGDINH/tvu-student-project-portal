/* eslint-disable react-hooks/set-state-in-effect */
import{useEffect,useState}from'react';import{availableCourseClasses,selfEnroll}from'../../api/courseEnrollmentApi'
export default function CourseClassEnrollmentPage(){const[items,setItems]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');const load=()=>{setLoading(true);availableCourseClasses().then(r=>{setItems(r.data||[]);setError('')}).catch(e=>setError(e.message)).finally(()=>setLoading(false))};useEffect(load,[]);if(loading)return<div className="skeleton-list">{[1,2,3].map(x=><div key={x}/>)}</div>;return<div className="lms-page"><div className="page-title"><h2>Đăng ký lớp học phần</h2><p>Đăng ký trực tiếp các lớp đang mở và còn chỗ.</p></div>{error&&<div className="alert error">{error}<button onClick={load}>Thử lại</button></div>}{!items.length?<div className="lms-empty"><h3>Hiện không có lớp đang mở</h3><p>Vui lòng quay lại trong thời gian đăng ký.</p></div>:<div className="course-grid">{items.map(x=><article className="course-card" key={x.id}><div className="course-card-body"><span className="course-code">{x.code}</span><h3>{x.subjectName}</h3><p>{x.semesterName} · {x.academicYearName}</p><p>Giảng viên: {x.lecturerCode} — {x.lecturerName}</p><p>Sĩ số: {x.enrollmentCount}/{x.capacity||'—'}</p><p>Mở: {x.enrollmentOpenAt?new Date(x.enrollmentOpenAt).toLocaleString('vi-VN'):'Không giới hạn'}</p><p>Đóng: {x.enrollmentCloseAt?new Date(x.enrollmentCloseAt).toLocaleString('vi-VN'):'Không giới hạn'}</p><button className="btn-primary" onClick={async()=>{try{await selfEnroll(x.id);load()}catch(e){setError(e.message)}}}>Đăng ký</button></div></article>)}</div>}</div>}
