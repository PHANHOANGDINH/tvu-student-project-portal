import{useEffect,useState}from'react';import{listStudentRequirements}from'../../api/submissionRequirementsApi';
export default function SubmissionRequirementsPage(){const[items,setItems]=useState([]),[selected,setSelected]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{listStudentRequirements().then(r=>setItems(r.data||[])).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <div><div className="page-title"><h2>Yêu cầu nộp bài</h2><p>Xem lịch, hạn nộp và tài liệu bắt buộc của các lớp đang tham gia.</p></div>{error&&<div className="alert error">{error}</div>}<div className="panel">{loading?<p>Đang tải...</p>:<table><thead><tr><th>Yêu cầu</th><th>Lớp</th><th>Hạn nộp</th><th>Trạng thái</th></tr></thead><tbody>{items.map(x=><tr key={x.id} onClick={()=>setSelected(x)}><td>{x.title}</td><td>{x.classCode}</td><td>{new Date(x.deadline).toLocaleString('vi-VN')}</td><td>{x.effectiveStatus}</td></tr>)}{!items.length&&<tr><td colSpan="4">Hiện chưa có yêu cầu nộp bài.</td></tr>}</tbody></table>}</div>{selected&&<div className="panel"><div className="row-between"><h3>{selected.title}</h3><button className="btn-light" onClick={()=>setSelected(null)}>Đóng</button></div><p>{selected.description}</p>{selected.instructions&&<p><strong>Hướng dẫn:</strong> {selected.instructions}</p>}<p><strong>Thời gian:</strong> {new Date(selected.startAt).toLocaleString('vi-VN')} – {new Date(selected.deadline).toLocaleString('vi-VN')}</p><p><strong>Số lần nộp tối đa:</strong> {selected.maxAttempts} · <strong>Nộp trễ:</strong> {selected.allowLate?'Có':'Không'} · <strong>Nộp lại:</strong> {selected.allowResubmission?'Có':'Không'}</p><p><strong>Tài liệu bắt buộc:</strong> {selected.requiredItems.map(i=>i.type).join(', ')}</p></div>}</div>}
