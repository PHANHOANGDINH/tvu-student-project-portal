import{useEffect,useState}from'react';import{useParams}from'react-router-dom';import{downloadSubmissionFile,lecturerSubmission}from'../../api/submissionsApi';
export default function SubmissionDetailPage(){const{id}=useParams(),[data,setData]=useState(null),[error,setError]=useState('');useEffect(()=>{lecturerSubmission(id).then(r=>setData(r.data)).catch(e=>setError(e.message))},[id]);const download=async f=>{try{const b=await downloadSubmissionFile(f.id);const u=URL.createObjectURL(b),a=document.createElement('a');a.href=u;a.download=f.originalName;a.click();URL.revokeObjectURL(u)}catch(e){setError(e.message)}};return <div><div className="page-title"><h2>Chi tiết bài nộp</h2><p>{data&&`${data.groupName} · ${data.classCode}`}</p></div>{error&&<div className="alert error">{error}</div>}{!data?<div className="panel">Đang tải...</div>:data.attempts.map(a=><div className="panel" key={a.id}><h3>Lần {a.attemptNumber} · {a.status}</h3><p>{new Date(a.submittedAt).toLocaleString('vi-VN')} {a.isLate?'· Trễ hạn':''}</p>{a.files.map(f=><p key={f.id}>{f.type}: <button onClick={()=>download(f)}>{f.originalName}</button> ({(Number(f.size)/1024/1024).toFixed(2)} MB)</p>)}{a.links.map(l=><p key={l.id}><a href={l.url} target="_blank" rel="noreferrer">{l.type}: {l.url}</a></p>)}</div>)}</div>}
