import { useState } from 'react';import { listGroups } from '../../api/groupsApi';
export default function LecturerGroupsPage(){const [classId,setClassId]=useState(''),[items,setItems]=useState([]),[error,setError]=useState('');const load=async()=>{try{setError('');setItems((await listGroups(classId)).data||[])}catch(e){setError(e.message)}};return <div><div className="page-title"><h2>Nhóm sinh viên</h2><p>Chỉ hiển thị nhóm thuộc lớp bạn phụ trách.</p></div>{error&&<div className="alert error">{error}</div>}<div className="panel"><div className="form-row"><input placeholder="Id lớp học phần" value={classId} onChange={e=>setClassId(e.target.value)}/><button className="btn-primary" onClick={load}>Xem nhóm</button></div><table><thead><tr><th>Nhóm</th><th>Lớp</th><th>Trưởng nhóm</th><th>Thành viên</th></tr></thead><tbody>{items.map(g=><tr key={g.id}><td>{g.name}</td><td>{g.classCode}</td><td>{g.leaderName}</td><td>{g.memberCount}/{g.maxMembers}</td></tr>)}{!items.length&&<tr><td colSpan="4">Chưa có dữ liệu.</td></tr>}</tbody></table></div></div>}
