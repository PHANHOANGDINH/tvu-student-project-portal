import{useState}from'react';import{useNavigate}from'react-router-dom';import{lecturerRequirementSubmissions}from'../../api/submissionsApi';
export default function SubmissionsPage(){const nav=useNavigate(),[rid,setRid]=useState(''),[status,setStatus]=useState(''),[items,setItems]=useState([]),[error,setError]=useState('');const load=()=>lecturerRequirementSubmissions(rid).then(r=>setItems(r.data||[])).catch(e=>setError(e.message));const shown=items.filter(x=>!status||x.status===status);return <div><div className="page-title"><h2>Bài nộp sinh viên</h2><p>Xem bài nộp thuộc yêu cầu và lớp bạn phụ trách.</p></div>{error&&<div className="alert error">{error}</div>}<div className="panel"><div className="form-row"><input placeholder="Id yêu cầu nộp" value={rid} onChange={e=>setRid(e.target.value)}/><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">Tất cả trạng thái</option><option>SUBMITTED</option><option>LATE</option><option>RESUBMITTED</option></select><button onClick={load}>Tải danh sách</button></div><table><thead><tr><th>Nhóm</th><th>Yêu cầu / lớp</th><th>Lần nộp</th><th>Trạng thái</th></tr></thead><tbody>{shown.map(x=><tr key={x.id} onClick={()=>nav(`/lecturer/submissions/${x.id}`)}><td>{x.groupName}</td><td>{x.requirementTitle} / {x.classCode}</td><td>{x.latestAttemptNumber}</td><td>{x.status}</td></tr>)}{!shown.length&&<tr><td colSpan="4">Chưa có bài nộp.</td></tr>}</tbody></table></div></div>}
