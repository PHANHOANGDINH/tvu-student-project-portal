import{useEffect,useState}from'react';import{useParams}from'react-router-dom';import{createCriterion,deleteCriterion,getCriteria,updateCriterion}from'../../api/gradingApi';const blank={name:'',description:'',maxScore:'',displayOrder:0};
export default function EvaluationCriteriaPage(){const{id}=useParams(),[items,setItems]=useState([]),[form,setForm]=useState(blank),[editing,setEditing]=useState(null),[error,setError]=useState('');const load=()=>getCriteria(id).then(r=>setItems(r.data||[])).catch(e=>setError(e.message));useEffect(load,[id]);const save=async e=>{e.preventDefault();try{editing?await updateCriterion(editing,form):await createCriterion(id,form);setEditing(null);setForm(blank);load()}catch(e){setError(e.message)}};const edit=x=>{setEditing(x.id);setForm(x)};return <div><div className="page-title"><h2>Tiêu chí chấm điểm</h2><p>Tổng điểm tối đa: {items.reduce((n,x)=>n+Number(x.maxScore),0)}/10</p></div>{error&&<div className="alert error">{error}</div>}<form className="panel" onSubmit={save}><input required placeholder="Tên tiêu chí" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><textarea placeholder="Mô tả" value={form.description||''} onChange={e=>setForm({...form,description:e.target.value})}/><div className="form-row"><input required type="number" min="0.01" step="0.01" placeholder="Điểm tối đa" value={form.maxScore} onChange={e=>setForm({...form,maxScore:e.target.value})}/><input type="number" min="0" placeholder="Thứ tự" value={form.displayOrder} onChange={e=>setForm({...form,displayOrder:e.target.value})}/></div><button className="btn-primary">{editing?'Cập nhật':'Thêm tiêu chí'}</button></form><div className="panel"><table><thead><tr><th>Tiêu chí</th><th>Điểm tối đa</th><th>Thứ tự</th><th></th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><strong>{x.name}</strong><small>{x.description}</small></td><td>{x.maxScore}</td><td>{x.displayOrder}</td><td><button onClick={()=>edit(x)}>Sửa</button><button onClick={()=>deleteCriterion(x.id).then(load).catch(e=>setError(e.message))}>Xóa</button></td></tr>)}{!items.length&&<tr><td colSpan="4">Chưa có tiêu chí.</td></tr>}</tbody></table></div></div>}
