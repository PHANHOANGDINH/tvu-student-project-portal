import{buildApiUrl,request}from'./http';export const currentSubmission=id=>request(`/student/submission-requirements/${id}/submission`);export function uploadSubmission(id,form,onProgress){return new Promise((resolve,reject)=>{const x=new XMLHttpRequest();x.open('POST',buildApiUrl(`/student/submission-requirements/${id}/submissions`));x.setRequestHeader('Authorization',`Bearer ${localStorage.getItem('access_token')||''}`);x.upload.onprogress=e=>e.lengthComputable&&onProgress?.(Math.round(e.loaded*100/e.total));x.onload=()=>{let d;try{d=JSON.parse(x.responseText)}catch{d=null}x.status>=200&&x.status<300?resolve(d):reject(new Error(d?.message||'Upload thất bại'))};x.onerror=()=>reject(new Error('Không thể kết nối máy chủ'));x.send(form)})}export const studentSubmission=id=>request(`/student/submissions/${id}`);export const studentHistory=id=>request(`/student/submissions/${id}/history`);export const lecturerRequirementSubmissions=id=>request(`/lecturer/submission-requirements/${id}/submissions`);export const lecturerSubmission=id=>request(`/lecturer/submissions/${id}`);export const lecturerHistory=id=>request(`/lecturer/submissions/${id}/history`);export async function downloadSubmissionFile(id,role='lecturer'){const r=await fetch(buildApiUrl(`/${role}/submission-files/${id}/download`),{headers:{Authorization:`Bearer ${localStorage.getItem('access_token')||''}`}});if(!r.ok)throw new Error('Không thể tải file');return r.blob()}
