import express from 'express';import multer from 'multer';import auth from '../../middlewares/auth.middleware.js';import role from '../../middlewares/role.middleware.js';import {USER_ROLES} from '../../constants/roles.js';import * as controller from './lecturerImport.controller.js';
const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:2*1024*1024,files:1},fileFilter(req,file,callback){const name=file.originalname.toLowerCase().endsWith('.csv'),type=['text/csv','application/csv','application/vnd.ms-excel'].includes(file.mimetype);callback(name&&type?null:new multer.MulterError('LIMIT_UNEXPECTED_FILE','file'),name&&type);}});const csv=(req,res,next)=>upload.single('file')(req,res,error=>error?res.status(error.code==='LIMIT_FILE_SIZE'?413:400).json({success:false,message:error.code==='LIMIT_FILE_SIZE'?'File CSV vượt quá 2 MB.':'Chỉ chấp nhận một file .csv.',data:null,errors:null}):next());
const router=express.Router();router.use(auth,role(USER_ROLES.ADMIN));router.get('/import-template',controller.template);router.post('/import-preview',csv,controller.preview);router.post('/import-confirm',controller.confirm);router.get('/export',controller.exportLecturers);export default router;
