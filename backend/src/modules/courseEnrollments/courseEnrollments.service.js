import*as r from'./courseEnrollments.repository.js';const ok=(data,message,statusCode=200)=>({success:true,data,message,statusCode}),fail=(statusCode,message)=>({success:false,statusCode,message}),id=x=>Number.isInteger(Number(x))&&Number(x)>0?Number(x):null
export const available=async u=>ok(await r.available(u.id),'Lấy lớp đang mở thành công')
export async function enroll(cid,u){if(!id(cid))return fail(400,'Lớp học phần không hợp lệ.');try{await r.enroll(id(cid),u.id);return ok(null,'Đăng ký lớp học phần thành công.',201)}catch(e){return fail(e.code==='NOT_FOUND'?404:['DUPLICATE','CAPACITY'].includes(e.code)?409:403,e.message)}}
export async function withdraw(cid,u){try{return await r.withdraw(id(cid),u.id)?ok(null,'Hủy đăng ký thành công.'):fail(404,'Không tìm thấy enrollment active.')}catch(e){return fail(409,e.message)}}
export async function transfer(body,u){if(!id(body.studentId)||!id(body.sourceCourseClassId)||!id(body.targetCourseClassId)||!String(body.reason||'').trim())return fail(400,'Sinh viên, lớp nguồn, lớp đích và lý do là bắt buộc.');try{await r.transfer(id(body.studentId),id(body.sourceCourseClassId),id(body.targetCourseClassId),u.id);return ok(null,'Chuyển lớp học phần thành công.')}catch(e){return fail(e.code==='NOT_FOUND'?404:409,e.message)}}
