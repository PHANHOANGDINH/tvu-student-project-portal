import express from'express';import auth from'../../middlewares/auth.middleware.js';import role from'../../middlewares/role.middleware.js';import{USER_ROLES as R}from'../../constants/roles.js';import * as c from'./courseEnrollments.controller.js';export const studentEnrollmentRoutes=express.Router();studentEnrollmentRoutes.use(auth,role(R.STUDENT));studentEnrollmentRoutes.get('/available',c.available);studentEnrollmentRoutes.route('/:id').post(c.enroll).delete(c.withdraw);export const adminEnrollmentRoutes=express.Router();adminEnrollmentRoutes.use(auth,role(R.ADMIN));adminEnrollmentRoutes.post('/transfer',c.transfer)
