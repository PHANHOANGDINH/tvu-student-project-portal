import express from'express';import auth from'../../middlewares/auth.middleware.js';import role from'../../middlewares/role.middleware.js';import{USER_ROLES as R}from'../../constants/roles.js';import * as c from'./dashboard.controller.js';export const adminDashboardRoutes=express.Router().get('/',auth,role(R.ADMIN),c.admin);export const lecturerDashboardRoutes=express.Router().get('/',auth,role(R.LECTURER),c.lecturer);export const studentDashboardRoutes=express.Router().get('/',auth,role(R.STUDENT),c.student);
