import{sendError,sendSuccess}from'../../utils/apiResponse.util.js';import * as s from'./dashboard.service.js';const run=fn=>async(req,res)=>{try{return sendSuccess(res,await fn(req.user))}catch(e){console.error(e);return sendError(res,{statusCode:500,message:'Lỗi hệ thống'})}};export const admin=run(()=>s.adminDashboard());export const lecturer=run(u=>s.lecturerDashboard(u));export const student=run(u=>s.studentDashboard(u));
